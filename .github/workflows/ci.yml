name: CI

on:
  push:
    branches: [main, master, "claude/**"]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  schema-and-yaml-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Validate JSON schemas
        run: |
          set -euo pipefail
          sudo apt-get update -y
          sudo apt-get install -y jq
          for f in schemas/*.json; do
            echo "Validating $f"; jq . "$f" >/dev/null; done
      - name: Validate JSON docs
        run: |
          set -euo pipefail
          for f in $(git ls-files '*.json' ':!:schemas/*.json'); do
            echo "Validating $f"; jq . "$f" >/dev/null || true; done
      - name: Validate YAML files
        run: |
          set -euo pipefail
          python3 - <<'PY'
import sys, pathlib, yaml
base = pathlib.Path('.')
for p in list(base.rglob('*.yml')) + list(base.rglob('*.yaml')):
    try:
        yaml.safe_load(p.read_text())
        print(f"OK {p}")
    except Exception as e:
        print(f"YAML error {p}: {e}")
        sys.exit(1)
PY

  lint-typecheck-test:
    name: Lint, Typecheck & Test (Node.js)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af # v4.1.0
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Tests
        run: npm run test:ci
